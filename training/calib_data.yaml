# Calibration dataset for int8 TFLite export (banknote images — representative
# for both denomination2 and securitycf forward passes). Labels are unused for
# int8 calibration; only the images feed the quantization statistics.
path: D:/Github/Billsense-Project/training/calib_subset
train: images
val: images
nc: 6
names: ['20', '50', '100', '200', '500', '1000']
